//초기상태값
const initialState = ["새우살", "살치살", "부채살", "안심", "채끝", "기타"]

//reducer :함수
//input:state, action

//action:setState동작
const MEATS = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }

}
export default MEATS